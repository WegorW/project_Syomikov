'''Дана строка. Преобразовать в ней все строчные буквы (как латинские,
 так и русские) в прописные, а прописные — в строчные.'''

def swap_case_custom(s: str) -> str:
    result = ""
    i = 0

    while i < len(s):
        char = s[i]
        if char.islower():
            result += char.upper()
        elif char.isupper():
            result += char.lower()
        else:
            result += char

        i += 1

    return result

print(f" Исходная строка: '{S1}'. Преобразованная строка: '{R1}'")

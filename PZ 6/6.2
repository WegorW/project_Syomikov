 '''Дано число R и список N. Найти два различных элемента списка, сумма
 которых наиболее близка к числу R, и вывести эти элементы в порядке возрастания.'''
 
import random

def find_closest_sum_pair(N_list):
    # Создаем случайный список
    A = [random.randint(-50, 50) for _ in range(N_list)]
    # Создаем случайное число R
    R = random.randint(-100, 100)
    
    min_diff = float('inf')
    best_pair = None

    i = 0
    while i < N_list:
        j = i + 1
        while j < N_list:
            sum_val = A[i] + A[j]
            diff = abs(sum_val - R)

            if diff < min_diff:
                min_diff = diff
                # Сохраняем пару в правильном порядке
                if A[i] <= A[j]:
                    best_pair = (A[i], A[j])
                else:
                    best_pair = (A[j], A[i])

            j += 1
        i += 1

    return A, R, best_pair

N1 = 10
A1, R1, best_pair1 = find_closest_sum_pair(N1)
elements_str1 = f"({best_pair1[0]}, {best_pair1[1]})" if best_pair1 else "Не найдено"
print(f"Список A={A1}")
print(f"Число R={R1}")
print(f"Наиболее близкие элементы по сумме: {elements_str1}")
print()

N2 = 15
A2, R2, best_pair2 = find_closest_sum_pair(N2)
elements_str2 = f"({best_pair2[0]}, {best_pair2[1]})" if best_pair2 else "Не найдено"
print(f"Список A={A2}")
print(f"Число R={R2}")
print(f"Наиболее близкие элементы по сумме: {elements_str2}")
